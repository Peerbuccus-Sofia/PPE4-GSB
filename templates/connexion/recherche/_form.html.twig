<div class="jumbotron">
                <div class="containter">
                    {{ form_start(formR)}}
                    <div class="row">
                        <div class="col-2">Prix</div>
                        <div class="col-2">Prix</div>
                        <div class="col-2"></div>
                        <div class="col-2">Type</div>
                        <div class="col-2">Ville</div>
                        <div class="col-2">Arrondissement</div>
                    </div>
                    <div class="form-row">
                        <div class="col-2">{{ form_widget(formR.minprix) }}</div>
                        <div class="col-2">{{ form_widget(formR.maxprix) }}</div>
                        <div class="col-2">{{ form_widget(formR.taille) }}</div>
                        <div class="col-2">{{ form_widget(formR.typeappart) }}</div>
                        <div class="col-2">{{ form_widget(formR.ville) }}</div>
                        <div class="col-2"> <button class="btn btn-info">Recherche</button></div>   
                    </div>
                {{ form_end(formR)}}
            </div>
        </div>